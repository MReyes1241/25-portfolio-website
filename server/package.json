{
  "name": "portfolio-contact-server",
  "version": "1.0.0",
  "description": "Backend server for portfolio contact form",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install && cp .env.template .env && echo 'Please configure your .env file'",
    "lint": "echo 'Add ESLint later'",
    "check-env": "node -e \"require('dotenv').config(); console.log('Environment variables loaded:', !!process.env.EMAIL_USER);\""
  },
  "keywords": [
    "portfolio",
    "contact",
    "express",
    "nodemailer"
  ],
  "author": "Manuel Reyes",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.7",
    "cors": "^2.8.5",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "validator": "^13.11.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}